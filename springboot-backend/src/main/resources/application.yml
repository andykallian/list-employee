spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ems?useTimezone=true&serverTimezone=UTC
    username: root
    password: 31052867
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
      show-sql: false